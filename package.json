{
  "name": "atria-android",
  "private": true,
  "version": "0.13.0",
  "description": "Atria Android wrapper for the v0.13 PWA",
  "scripts": {
    "prepare:web": "node scripts/prepare-web.mjs",
    "test:backend-backup": "node scripts/backend-backup-smoke.mjs",
    "android:add": "cap add android",
    "android:sync": "npm run prepare:web && cap sync android",
    "android:open": "cap open android",
    "android:run": "cap run android",
    "doctor": "cap doctor"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
